module.exports = verifyPostToken = (body) => {
  console.log([body.process, body.datos, body.token]);
  if (
    typeof body.process !== "undefined" &&
    typeof body.datos !== "undefined" &&
    typeof body.token !== "undefined"
  ) {
    return true;
  } else {
    return false;
  }
};
